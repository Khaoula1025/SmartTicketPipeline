apiVersion: batch/v1
kind: Job
metadata:
  name: ticket-prediction-batch
spec:
  template:
    spec:
      containers:
      - name: classifier
        image: ticket-classifier:latest
        imagePullPolicy: Never
        command: ["python", "src/modeling/predict.py"]
      restartPolicy: Never
  backoffLimit: 2
